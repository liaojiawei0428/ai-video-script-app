# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
build/
dist/
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*

# Testing
coverage/

# React Native
.expo/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Temporary files
*.tmp
*.temp
# S74 bump-version.py 自动备份文件 (跟 S73 v3.0.82 BUG-159 教训, 防误 commit)
*.bak

# Uploaded files (any level)
**/uploads/*
!**/uploads/.gitkeep

# Database
data/
*.db
*.db-journal

# Build artifacts
dist.zip

# Android Gradle cache
apps/mobile/android/.gradle/
apps/mobile/android/app/debug.keystore
apps/mobile/android/app/build/

# Misc
AiScriptMobile

codegraph/
.codegraph/

# === Temp artifacts (root only, added 2026-06-18 to fix push size) ===
/*.png
/*.jpg
/*.jpeg
/*.gif
/*.mp4
/*.webm
/*.mov
/*.gz
/*.tar
/*.zip
/*.rar
/WEB_DEPLOY_STEPS.md
/yavg.txt
/login-body.json

# === S72 batch 6 清理 (2026-06-26) ===
# NUL: Windows reserved name, 物理不存在但 git 误报 untracked
/NUL
# deploy.sh.original: S72 batch 4 损坏版 backup, 保留文件但不进 git
apps/server/deploy.sh.original

# === S72 batch 7 部署临时构建产物 (2026-06-26) ===
# 部署前 npm run build 打的 dist tar, 部署完即弃 (跟 S72 batch 6 21 untracked 清理同源)
apps/server/dist_*.tar.gz
apps/web/web-dist-*.tgz

# === S72 batch 30 BUG-130 (2026-06-30) ===
# adb screenshots: temp PNGs from BUG verification, 100+ files, not in git
.adb-shots/

# === S85 BUG-182 (2026-07-07) ===
# tools/restart-and-verify.sh + apps/tools/: 临时 verify 脚本 + 临时目录, 实战用, 不进 git
# 跟 shipin-APP deploy.sh 12 维验证重叠, 重复; 如果需要, 后续集成进 deploy.sh 而不是单独 shell
tools/restart-and-verify.sh
apps/tools/
tools/verify-v3.0.101-public.py

# === S85 BUG-182 APK (2026-07-07) ===
# 本机构建产物, 公网已 scp, 不进 git (类似 deepscript_v3.0.*.apk 进 .gitignore)
DeepScript_v3.0.104.apk
